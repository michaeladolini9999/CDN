valid_referers server_names
    ~^(?:https?://)?([^/]*ezball[^/]*)/
    ~^(?:https?://)?([^/]*sedbergh[^/]*)/
#    ~sedbergh
#    ~^(?:https?://)?(?:.*\.)?[^/]*sedbergh[^/]*\.vn(?::\d+)?(?:/.*)?$
#    *.globalup.site
    *.livecdn.site;

if ($invalid_referer) {
    return 404;
}
